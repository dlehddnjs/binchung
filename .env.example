EVCHARGER_API_KEY=

# 로컬 docker-compose 기본값 (docker-compose.yml과 동일한 더미 자격증명)
DATABASE_URL=postgres://binchung:binchung_dev_only_password@localhost:5432/binchung

# fixtures(기본, 실 API 호출 안 함) | live(실제 환경부 API 호출)
COLLECTOR_MODE=fixtures
